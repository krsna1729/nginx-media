# Scenario: API only.
#
# The smallest useful configuration.  No ingest at all - the program is built
# entirely through the control API and fed with a file source, which is the
# quickest way to exercise the runtime graph, the selector and the HLS output
# without a publisher.
#
#   docker run -d --name media -p 8080:8080 \
#     -v "$PWD/container/examples/01-api-only.conf:/etc/nginx/nginx.conf:ro" \
#     -v media:/var/lib/nginx/media \
#     ghcr.io/krsna1729/nginx-media:latest
#
#   curl -X POST -H 'Content-Type: application/json' \
#     -d '{"application":"live","name":"demo"}' \
#     http://127.0.0.1:8080/media/api/v1/streams
#
#   docker cp sample.ts media:/sample.ts
#   curl -X POST -H 'Content-Type: application/json' \
#     -d '{"id":"file1","type":"file","path":"/sample.ts"}' \
#     http://127.0.0.1:8080/media/api/v1/streams/live/demo/sources
#
#   curl http://127.0.0.1:8080/hls/index.m3u8

user www-data;
include /etc/nginx/conf.d/workers.conf;

error_log /var/log/nginx/error.log info;
# Where nginx looks for its own pid: the compiled prefix.  Keeping it
# here rather than /run means `nginx -s reload` works without also
# having to name the configuration file.
pid /usr/local/nginx/logs/nginx.pid;

events {
    worker_connections 1024;
}

media_hls /var/lib/nginx/media/hls;

http {
    access_log off;

    server {
        listen 8080 reuseport;

        location /media/api/ { media_api; }
        location /hls/       { alias /var/lib/nginx/media/hls/; }
    }
}
